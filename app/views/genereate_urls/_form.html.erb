<%= form_for(@genereate_url) do |f| %>
  <% if @genereate_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@genereate_url.errors.count, "error") %> prohibited this genereate_url from being saved:</h2>

      <ul>
      <% @genereate_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url_name %><br>
    <%= f.text_field :url_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
